{
  "scripts": {
    "cypress:open": "NO_COLOR=1 cypress open -b chrome --config baseUrl=https://dw.beliana.sav.sk",
    "cypress:run": "NO_COLOR=1 cypress run -b chrome --config baseUrl=https://dw.beliana.sav.sk",
    "cypress:run:specificTest": "NO_COLOR=1 cypress run --spec '' -b chrome --config baseUrl=https://dw.beliana.sav.sk"
  },
  "devDependencies": {
    "cypress": "^13.15.0",
    "cypress-map": "^1.30.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "cypress-plugin-steps": "^1.1.1"
  }
}
